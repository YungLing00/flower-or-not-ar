const $=(s,c=document)=>c.querySelector(s),$$=(s,c=document)=>[...c.querySelectorAll(s)];const toast=$('#toast');
function getProgress(){return JSON.parse(localStorage.getItem('flower-web-missions')||'[false,false,false,false]')}
function render(){const p=getProgress(),n=p.filter(Boolean).length;$('#totalBadges').textContent=n;$('#progressText').textContent=n+' / 4 COMPLETE';$('#progressFill').style.width=(n/4*100)+'%';$$('.task-card').forEach((c,i)=>{c.classList.toggle('done',p[i]);$('.status',c).textContent=p[i]?'COMPLETE ✓':'NOT CLEARED'});$('#allClear').classList.toggle('show',n===4)}
function showToast(t){toast.textContent=t;toast.classList.add('show');clearTimeout(showToast.t);showToast.t=setTimeout(()=>toast.classList.remove('show'),1600)}
$('#clearProgress').onclick=()=>{localStorage.setItem('flower-web-missions','[false,false,false,false]');render();showToast('四個任務徽章已清除')};window.addEventListener('pageshow',render);window.addEventListener('storage',render);render();